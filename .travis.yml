language: python
sudo: false

python:
    - 3.6
    - 3.7

before_install:
    - pip install pytest pytest-cov pytest-benchmark codecov

install:
    - pip install cplex python-libsbml-experimental
    - pip install -e .

script:
    - py.test --benchmark-skip --cov=micom

after_success:
    - codecov

notifications:
    email: false
